{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "if test -f frontend/package.json; then npm ci --prefix frontend; else npm ci; fi",
  "buildCommand": "if test -f frontend/package.json; then npm run build --prefix frontend && rm -rf dist && cp -R frontend/dist dist; else npm run build; fi",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
